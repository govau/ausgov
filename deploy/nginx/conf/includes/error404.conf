# Editing the default nginx.conf is depreciated when using the staticfile_buildpack
# https://github.com/cloudfoundry/nginx-buildpack/blob/master/fixtures/mainline/nginx.conf
# Instead we're instructing the buildpack to look for /404.html from root folder.

error_page 404 404.html;
location = 404.html {
  internal;
}